package com.example.digi_dexproject.ui

import androidx.fragment.app.Fragment

/*

In the Fragment where a user scans a card (e.g., ScanFragment),
you must call onCardScanned() after a successful scan to reset the timer.

// In ScanFragment, after a scan is successful:
(activity as? MainActivity)?.onCardScanned()

 */



class ScanFragment : Fragment() {
}